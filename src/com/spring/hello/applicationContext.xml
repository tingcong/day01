<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--IOC容器的配置，要创建的所有对象都配置在这里 (根据id通过反射创建对象)-->
    <bean id="user" class="com.spring.hello.User" scope="singleton" lazy-init="true"  >
        <!--singleton:在启动(容器初始化之前)， 就已经创建了bean，且整个应用只有一个;默认值， 即 默认是单例	【service/dao/工具类】-->
        <!--prototype:在用到对象的时候，才创建对象; 多例； 				【Action对象】-->
        <!--lazy-init（只对singleton有效）:默认为false, false:不延迟创建，即在启动时候就创建对象; true:延迟初始化， 在用到对象的时候才创建对象-->
    </bean>
</beans>